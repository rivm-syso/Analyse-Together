Prepped data directory
-----------------

This directory contains prepped data, like lists of municipalities,
projects etc. These data is, for example, used in pull down menu's and
selection options.
